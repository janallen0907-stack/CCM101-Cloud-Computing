# Infrastructure Report

## 1. Operating System

The server is running Ubuntu 24.04.4 LTS with the codename Noble Numbat.

## 2. Kernel Version

The Linux kernel version is:

`6.8.0-136-generic`

## 3. CPU

The server is using an Intel Xeon E312xx processor from the Sandy Bridge family.

The system reports 1 CPU, with 1 core per socket and 1 thread per core.

## 4. Memory

The server has approximately 1.9 GiB of RAM.

It also has 1.0 GiB of swap space.

## 5. Storage

The main disk has a capacity of 20 GB.

The main root partition is approximately 19 GB and is mounted at `/`.

The server also has separate partitions for `/boot` and `/boot/efi`.

## 6. Mounted File Systems

The server has several mounted filesystems.

The main root filesystem uses the `ext4` filesystem and is mounted at `/`.

Other mounted filesystems include `/boot`, `/boot/efi`, `/run`, `/dev`, `/proc`, and other system-related virtual filesystems.

## 7. Hostname

The hostname of the server is:

`ubuntu`

## 8. IP Address

The server returned the following IP addresses:

- `172.30.1.2`
- `172.17.0.1`

## 9. Summary

The investigated server is a small Linux-based cloud environment running Ubuntu 24.04.4 LTS. It has one CPU, approximately 1.9 GiB of RAM, and a 20 GB disk.

The information gathered from the server will be used as the basis for planning the cloud infrastructure for CloudNova Technologies.
